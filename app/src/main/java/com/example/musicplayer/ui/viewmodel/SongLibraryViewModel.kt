package com.example.musicplayer.ui.viewmodel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class SongLibraryViewModel(application: Application) : AndroidViewModel(application) {
}